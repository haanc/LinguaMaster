fastapi
uvicorn
python-multipart
python-dotenv
sqlmodel
openai
yt-dlp
langchain>=0.1.0
langchain-openai
langchain-community
langchain-core
langgraph==1.0.5  # Pin to 1.0.5 - version 1.0.6 has missing langgraph.cache module
pydantic>=2.0
httpx

# Additional LLM Providers (for user-configurable LLM support)
langchain-anthropic>=0.2.0    # Claude/Anthropic support
langchain-google-genai>=2.0.0  # Gemini/Google support
langchain-ollama>=0.1.0        # Ollama local LLM support

# Local Whisper (optional - for offline transcription)
# Uncomment one of the following based on your hardware:
# faster-whisper              # CPU-only (uses int8 quantization)
# faster-whisper[gpu]         # NVIDIA GPU with CUDA support
faster-whisper

# Security
slowapi                        # Rate limiting for FastAPI

# Supabase (cloud auth and data)
supabase>=2.0.0               # Supabase Python client

# Export (Word document generation)
python-docx>=0.8.11           # Word document generation
