# ==============================================
# Fluent Learner V2 - Backend Configuration
# ==============================================
# Copy this file to .env and fill in your values
# NEVER commit .env files with real credentials!

# ⚠️ SECURITY NOTE:
# This backend runs LOCALLY inside the Electron app.
# Sensitive keys (SERVICE_ROLE_KEY, GEMINI_API_KEY, LEMONSQUEEZY)
# should be configured in Supabase Edge Functions, NOT here.
# See: supabase/functions/README.md

# ----------------------------------------------
# Local AI Provider (for users with own API keys)
# ----------------------------------------------
# These are only used when user provides their own API key
# via the Settings UI. The keys are stored in localStorage
# and passed via HTTP headers, not from these env vars.

# Uncomment to set a DEFAULT provider for development:
# LLM_PROVIDER=azure        # Options: azure, openai, ollama
# WHISPER_PROVIDER=local    # Options: azure, openai, local

# ----------------------------------------------
# Local Whisper Configuration (Offline Transcription)
# ----------------------------------------------
# Uses faster-whisper for local speech-to-text
# Models are downloaded automatically on first use
# This is safe - runs entirely on user's machine

# Model options: tiny (75MB), base (142MB), small (466MB), medium (1.5GB), large-v3 (2.9GB)
LOCAL_WHISPER_MODEL=base

# Device: auto (recommended), cuda (NVIDIA GPU), cpu
LOCAL_WHISPER_DEVICE=auto

# Compute type: auto (recommended), float16 (GPU), int8 (CPU), float32
LOCAL_WHISPER_COMPUTE_TYPE=auto

# Custom models directory (optional, defaults to app data folder)
# LOCAL_WHISPER_MODELS_DIR=/path/to/models

# ----------------------------------------------
# Supabase Configuration (Public keys only!)
# ----------------------------------------------
# ANON_KEY is safe to include - it's a public key
# with Row Level Security (RLS) protection
# Get from: https://supabase.com -> Project Settings -> API

SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ⚠️ DO NOT include SERVICE_ROLE_KEY here!
# It should only be in Supabase Edge Functions secrets.
